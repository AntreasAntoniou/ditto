# Python dependencies for restoring + converting the on-device ogma models to
# CoreML — used by tools/restore-models.sh (which calls tools/_dl.py and
# tools/convert_ogma.py) and by the CI workflows. Target: Python 3.11.
#
# Unpinned for the first run so pip resolves a mutually-compatible latest set.
# REPRODUCIBILITY: after your first green `Models` workflow run, freeze these to
# the resolved versions (copy from the run's `pip freeze`) so releases convert
# against a known-good stack and never drift.
torch
transformers
coremltools
sentencepiece
safetensors
huggingface_hub
